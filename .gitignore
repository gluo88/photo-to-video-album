# Ignores all Vim/editor backup files ending in .un~
.*.un~

# Ignores the folder and everything inside it
yaml_template/
tmp/
__pycache__/

# Local release notes (kept locally, not version controlled)
release_notes/
CLAUDE.md

# Tracked only in local git (not pushed to GitHub)
photo_geo.py
run_photo_geo.sh
